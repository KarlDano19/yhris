================================================================================
                            BUG HISTORY TRACKER
================================================================================

INSTRUCTIONS FOR CLAUDE CODE:
When a developer requests to document a bug fix, APPEND a new entry to the end
of this file following the template format below. Always maintain chronological
order with newest entries at the bottom. Preserve all existing entries.

Required information to collect:
- Bug ID (unique identifier)
- Date Filed (when bug was reported)
- Date Fixed (when fix was committed)
- Client/Reporter (who reported it)
- Issue Description (what was wrong)
- Desired Outcome (what should happen)
- Files Changed (list all modified files)
- Unit Testing Checklist (tests written, coverage, regression tests)
- Fix Explanation (how it was resolved)

================================================================================
TECH STACK REFERENCE - YAHSHUA HRIS
================================================================================

BACKEND (Django REST Framework):
- Django 4.2 / Django REST Framework 3.14.0
- PostgreSQL (psycopg2-binary)
- Redis (django-redis) for caching
- Celery 5.2.7 / Django Celery Beat for background tasks
- Django Channels 4.0.0 for WebSockets
- JWT authentication (djangorestframework-simplejwt)
- WeasyPrint for PDF generation
- Anthropic Claude AI for resume parsing
- AWS S3 (boto3, django-storages) for file storage

FRONTEND (Next.js/React):
- Next.js 14.1.1 (App Router)
- React 18.2.0 / TypeScript 5.0.4
- TanStack React Query 4.29.5 (server state management)
- React Hook Form 7.43.9 (form state management)
- React useState/useReducer (local component state)
- Tailwind CSS 3.3.2
- Headless UI / Heroicons
- Axios for API calls

================================================================================
TEMPLATE FORMAT:
================================================================================

BUG ID: [Unique identifier, e.g., BUG-001, HRIS-123]
DATE FILED: [YYYY-MM-DD]
DATE FIXED: [YYYY-MM-DD]
CLIENT/REPORTER: [Client name or internal reporter]

ISSUE DESCRIPTION:
[Clear description of the problem, including steps to reproduce if applicable]

DESIRED OUTCOME:
[What the expected behavior should be]

FILES CHANGED:
Backend:
- app/views/example.py
- app/serializers/example.py
- app/models/example.py

Frontend:
- src/app/example/page.tsx
- src/components/example/ExampleComponent.tsx
- src/hooks/useExample.ts

UNIT TESTING CHECKLIST:

Backend (Django REST Framework):
☐ Python unit tests written/updated (Django TestCase or APITestCase)
☐ All backend tests passing: python manage.py test
☐ Backend test coverage: [percentage] - Run: coverage run --source='.' manage.py test && coverage report
☐ Models tested (validation, methods, relationships, signals)
☐ Serializers tested (field validation, nested data, custom methods)
☐ Views/API endpoints tested (GET, POST, PUT, PATCH, DELETE responses)
☐ Permissions tested (authentication, authorization, role-based access)
☐ Celery tasks tested (task execution, error handling, retries)
☐ Database migrations tested (forward/backward compatibility)

Frontend (Next.js/React/TypeScript):
☐ Jest/React Testing Library tests written/updated
☐ All frontend tests passing: npm test or yarn test
☐ Frontend test coverage: [percentage] - Check coverage report
☐ Components tested (rendering, props, state changes)
☐ Custom hooks tested (useQuery, useMutation wrappers)
☐ React Query hooks tested (queries, mutations, cache invalidation)
☐ Form validation tested (React Hook Form rules, error states)
☐ API service functions tested (Axios calls, error handling)
☐ TypeScript types verified (no type errors)

Integration & E2E:
☐ API integration tests completed (frontend ↔ backend)
☐ Cypress/Playwright e2e tests updated (if applicable)
☐ Manual regression testing completed
☐ Edge cases and error scenarios tested
☐ Cross-browser testing completed (if UI changes)
☐ Mobile responsiveness verified (if UI changes)

FIX EXPLANATION:
[Detailed explanation of the changes made and why they resolve the issue]

================================================================================

BUG ID: EXAMPLE-001
DATE FILED: 2024-12-15
DATE FIXED: 2024-12-20
CLIENT/REPORTER: QA Team - Maria Santos

ISSUE DESCRIPTION:
Employee sync from HRIS to Payroll was failing when department name contained
special characters (e.g., "R&D Department", "Sales & Marketing"). The sync
process would throw a validation error and skip the employee record silently.

DESIRED OUTCOME:
Employee sync should handle all valid department names including those with
special characters. Any sync errors should be logged and reported clearly.

FILES CHANGED:
Backend:
- app/views/yahshua_payroll.py
- app/serializers/employee.py
- utils/oauth.py
- tests/views/test_yahshua_payroll.py

Frontend:
- src/app/(employer)/settings/integration/payroll-sync/page.tsx
- src/components/settings/PayrollSyncStatus.tsx
- src/hooks/usePayrollSync.ts

UNIT TESTING CHECKLIST:

Backend (Django REST Framework):
☑ Python unit tests written/updated (APITestCase for sync endpoints)
☑ All backend tests passing: python manage.py test tests.views.test_yahshua_payroll
☑ Backend test coverage: 94% - sync views and serializers fully covered
☑ Models tested (N/A - no model changes)
☑ Serializers tested (special character handling in department field)
☑ Views/API endpoints tested (sync endpoint with various department names)
☑ Permissions tested (employer-only access verified)
☑ Celery tasks tested (N/A - sync is synchronous)
☑ Database migrations tested (N/A - no schema changes)

Frontend (Next.js/React/TypeScript):
☐ Jest/React Testing Library tests written/updated (tests not yet implemented)
☐ All frontend tests passing: N/A
☐ Frontend test coverage: N/A
☑ Components tested (manual testing of PayrollSyncStatus)
☐ Custom hooks tested (usePayrollSync manual verification)
☐ React Query hooks tested (manual cache invalidation check)
☐ Form validation tested (N/A - no form changes)
☐ API service functions tested (manual API call verification)
☑ TypeScript types verified (no type errors)

Integration & E2E:
☑ API integration tests completed (verified sync with Payroll staging)
☐ Cypress/Playwright e2e tests updated (e2e not yet implemented)
☑ Manual regression testing completed
☑ Edge cases tested (special chars: &, ', ", <, >, unicode)
☐ Cross-browser testing completed (N/A - no UI changes)
☐ Mobile responsiveness verified (N/A - settings page desktop-only)

FIX EXPLANATION:
The issue was caused by URL encoding not being applied to department names
before sending to the Payroll API. Updated utils/oauth.py to properly encode
all string parameters using urllib.parse.quote(). Added explicit error logging
in the sync view to capture and report validation failures. Frontend sync
status component now displays detailed error messages when sync fails,
allowing users to identify and fix problematic records.

================================================================================

BUG ID: EXAMPLE-002
DATE FILED: 2024-12-18
DATE FIXED: 2024-12-22
CLIENT/REPORTER: Systech Internal - Dev Team

ISSUE DESCRIPTION:
Notification bell showing incorrect unread count after marking notifications
as read. The count would only update after page refresh, and sometimes show
negative numbers when rapidly clicking notifications.

DESIRED OUTCOME:
Notification count should update instantly when notifications are marked as
read, with optimistic updates and proper error handling. Count should never
show negative values.

FILES CHANGED:
Backend:
- app/views/notification.py
- app/serializers/notification.py
- tests/views/test_notification.py

Frontend:
- src/components/layout/NotificationBell.tsx
- src/components/notifications/NotificationList.tsx
- src/hooks/useNotifications.ts

UNIT TESTING CHECKLIST:

Backend (Django REST Framework):
☑ Python unit tests written/updated (APITestCase for notification endpoints)
☑ All backend tests passing: python manage.py test tests.views.test_notification
☑ Backend test coverage: 91% - notification views covered
☑ Models tested (notification count aggregation)
☑ Serializers tested (unread count serialization)
☑ Views/API endpoints tested (mark as read, bulk mark, count endpoint)
☑ Permissions tested (user can only access own notifications)
☑ Celery tasks tested (N/A)
☑ Database migrations tested (N/A - no schema changes)

Frontend (Next.js/React/TypeScript):
☐ Jest/React Testing Library tests written/updated (tests not yet implemented)
☐ All frontend tests passing: N/A
☐ Frontend test coverage: N/A
☑ Components tested (manual testing of NotificationBell, NotificationList)
☑ Custom hooks tested (useNotifications optimistic updates verified)
☑ React Query hooks tested (cache invalidation on mark as read)
☐ Form validation tested (N/A)
☐ API service functions tested (manual verification)
☑ TypeScript types verified (no type errors)

Integration & E2E:
☑ API integration tests completed (rapid click scenarios)
☐ Cypress/Playwright e2e tests updated (e2e not yet implemented)
☑ Manual regression testing completed
☑ Edge cases tested (rapid clicks, network errors, empty state, concurrent tabs)
☑ Cross-browser testing completed (tested Chrome, Firefox, Safari)
☑ Mobile responsiveness verified (notification dropdown on mobile)

FIX EXPLANATION:
Implemented optimistic updates in useNotifications hook using React Query's
onMutate callback to immediately decrement count before API response. Added
Math.max(0, count) guard in the component's local state to prevent negative
values. Backend now returns updated unread_count in mark-as-read response for
reconciliation. Added debouncing (300ms) to prevent rapid-fire API calls
when clicking multiple notifications quickly. React Query's cache is now
properly invalidated after successful mutation to ensure consistency.

================================================================================

[NEW ENTRIES SHOULD BE APPENDED BELOW THIS LINE]

BUG ID: HRIS-001
DATE FILED: 2026-01-15
DATE FIXED: 2026-01-15
CLIENT/REPORTER: Production Client - Job Posting #276

ISSUE DESCRIPTION:
When applicants applied to a specific job posting (ID 276), the screen-applicants page
would crash with a client-side exception: "TypeError: Cannot read properties of null
(reading 'toLowerCase')". The error occurred during screening answer evaluation when
comparing applicant responses against ideal answers. The page displayed "Application
error: a client-side exception has occurred" and was completely unusable for that
job posting.

DESIRED OUTCOME:
The screen-applicants page should load and display all applicants regardless of
whether their screening answers contain null values. Applicants with null/missing
screening answers should be treated as "not fit" for those questions rather than
crashing the entire page.

FILES CHANGED:
Backend:
- (No backend changes required)

Frontend:
- src/components/pages/(auth)/employer/screen-applicants/[id]/Content.tsx

UNIT TESTING CHECKLIST:

Backend (Django REST Framework):
☐ Python unit tests written/updated (N/A - no backend changes)
☐ All backend tests passing (N/A)
☐ Backend test coverage: N/A
☐ Models tested (N/A)
☐ Serializers tested (N/A)
☐ Views/API endpoints tested (N/A)
☐ Permissions tested (N/A)
☐ Celery tasks tested (N/A)
☐ Database migrations tested (N/A)

Frontend (Next.js/React/TypeScript):
☐ Jest/React Testing Library tests written/updated (tests not yet implemented)
☐ All frontend tests passing: N/A
☐ Frontend test coverage: N/A
☑ Components tested (manual testing with job posting #276)
☐ Custom hooks tested (N/A - no hook changes)
☐ React Query hooks tested (N/A)
☐ Form validation tested (N/A)
☐ API service functions tested (N/A)
☑ TypeScript types verified (no type errors)

Integration & E2E:
☑ API integration tests completed (verified page loads with null screening data)
☐ Cypress/Playwright e2e tests updated (e2e not yet implemented)
☑ Manual regression testing completed
☑ Edge cases tested (null idealAnswer, null applicant answer, array with null values)
☐ Cross-browser testing completed (pending deployment)
☐ Mobile responsiveness verified (N/A - no UI changes)

FIX EXPLANATION:
The bug was in the `processApplicants` function (Content.tsx:230-254) which evaluates
applicant screening answers against the job posting's ideal answers. The code called
`.toLowerCase()` directly on values that could be null:

1. Multiple Choice comparison (line 237): `app.toLowerCase() === ideal.toLowerCase()`
   - Both `app` (applicant's answer) and `ideal` (ideal answer) could be null

2. Yes/No comparison (line 244): `applicantAnswer.toLowerCase() === idealAnswer.toLowerCase()`
   - Both values could be null if screening data was incomplete

The fix added null checks before calling `.toLowerCase()`:
- For Multiple Choice: Added guard `if (app == null || ideal == null) return false;`
  and wrapped values with `String()` for type safety
- For Yes/No: Added conditional check that sets `isMatch = false` when either value
  is null, otherwise performs the comparison with `String()` wrapper

This ensures applicants with null/missing screening answers are treated as "not fit"
for those specific questions (conservative approach) rather than crashing the page.
The root cause was likely incomplete screening question data in the database for
job posting #276.

================================================================================

FEATURE ID: HRIS-002
DATE REQUESTED: 2026-01-21
DATE IMPLEMENTED: 2026-01-21
CLIENT/REPORTER: Systech Internal - Feature Request

FEATURE DESCRIPTION:
Add CSV export functionality to the Individual Evaluations page. Currently, users
can only print evaluation data to PDF. The request was to allow users to choose
between PDF and CSV export formats when exporting individual evaluation records.

DESIRED OUTCOME:
Users should be able to:
1. Click the export button on the Individual Evaluations page
2. Select export format: PDF (existing) or CSV (new)
3. Select evaluation forms to include: All or Selected specific forms
4. Download the file in the chosen format

FILES CHANGED:
Backend:
- (No backend changes required - CSV generation handled on frontend)

Frontend:
- src/components/pages/(auth)/employer/manage/evaluation-history/tabs/IndividualEvaluations.tsx
- src/components/pages/(auth)/employer/manage/evaluation-history/modals/PrintIndividualEvaluationsSelectionModal.tsx

UNIT TESTING CHECKLIST:

Backend (Django REST Framework):
☐ Python unit tests written/updated (N/A - no backend changes)
☐ All backend tests passing (N/A)
☐ Backend test coverage: N/A
☐ Models tested (N/A)
☐ Serializers tested (N/A)
☐ Views/API endpoints tested (N/A)
☐ Permissions tested (N/A)
☐ Celery tasks tested (N/A)
☐ Database migrations tested (N/A)

Frontend (Next.js/React/TypeScript):
☐ Jest/React Testing Library tests written/updated (tests not yet implemented)
☐ All frontend tests passing: N/A
☐ Frontend test coverage: N/A
☑ Components tested (manual testing of export modal and CSV download)
☐ Custom hooks tested (N/A - no new hooks)
☐ React Query hooks tested (N/A)
☐ Form validation tested (N/A)
☐ API service functions tested (N/A)
☑ TypeScript types verified (no type errors, new ExportFormat type added)

Integration & E2E:
☑ API integration tests completed (verified data fetch for CSV export)
☐ Cypress/Playwright e2e tests updated (e2e not yet implemented)
☑ Manual regression testing completed
☑ Edge cases tested (empty data, special characters in names, large datasets)
☑ Cross-browser testing completed (CSV download works in Chrome, Firefox)
☐ Mobile responsiveness verified (modal responsive design maintained)

IMPLEMENTATION DETAILS:

1. PrintIndividualEvaluationsSelectionModal.tsx:
   - Added new `ExportFormat` type ('pdf' | 'csv') exported for reuse
   - Added format selection UI with radio buttons (PDF/CSV options)
   - Added DocumentArrowDownIcon and DocumentTextIcon for visual distinction
   - Updated `onConfirm` callback to include selected format parameter
   - Dynamic button text: "Print Report" for PDF, "Export CSV" for CSV
   - Added state management for `selectedFormat` with 'pdf' as default
   - Reset format selection on modal close

2. IndividualEvaluations.tsx:
   - Added `isExportingCSV` state for loading state management
   - Added `handleCSVExport` function for CSV generation:
     * Defines CSV headers: Employee Name, Date of Evaluation, Evaluation Period,
       Evaluation Form, Total Score, Max Score, Passing Score, Status
     * Converts data to CSV rows with proper escaping (quotes, special chars)
     * Creates Blob with CSV content and triggers browser download
     * Filename pattern: individual-evaluations-{from}-to-{to}.csv
   - Updated `handlePrintConfirm` to handle both PDF and CSV formats
   - Added tooltip for export button
   - Import ExportFormat type from modal component

CSV Export Format:
- Headers: Employee Name, Date of Evaluation, Evaluation Period, Evaluation Form,
          Total Score, Max Score, Passing Score, Status
- Status column: "Passed" if form_total_score >= passing_score, else "Failed"
- Special character handling: Values wrapped in quotes, internal quotes escaped
- File encoding: UTF-8 with proper MIME type (text/csv;charset=utf-8)

Design Decisions:
- Frontend-only CSV generation: Since data is already fetched when modal opens,
  generating CSV on frontend is efficient and avoids additional API calls
- Consistent filtering: Same form selection logic applies to both PDF and CSV
- User experience: Format selection appears first, then record selection
- Backward compatible: PDF remains the default format selection

================================================================================
