import Content from "@/components/pages/(auth)/employer/settings/general-settings/Content";
import React from "react";

export const metadata = {
  title: "General Settings - Yahshua HRIS",
};

const GeneralSettings = async () => {
  return <Content />;
};

export default GeneralSettings;
