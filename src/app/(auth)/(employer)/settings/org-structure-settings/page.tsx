import Content from "@/components/pages/(auth)/employer/settings/org-structure-settings/Content";
import React from "react";

export const metadata = {
  title: "Org Structure Settings - Yahshua HRIS",
};

const OrgStructureSettings = async () => {
  return <Content />;
};

export default OrgStructureSettings;
