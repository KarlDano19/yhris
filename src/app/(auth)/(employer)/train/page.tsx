import Content from "@/components/pages/(auth)/employer/train/Content";
import React from "react";

export const metadata = {
  title: "Train - Yahshua HRIS",
};

const Train = async () => {
  return <Content />;
};

export default Train;
