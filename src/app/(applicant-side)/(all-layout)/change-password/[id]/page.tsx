import Content from "@/components/pages/(applicant-side)/(all-layout)/all-change-password/Content";

export const metadata = {
  title: 'Change Password - Yahshua HRIS',
  description: 'HRIS',
};

const AllChangePassword = () => {
  return <Content />;
};

export default AllChangePassword;
