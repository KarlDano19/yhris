import Content from "@/components/pages/(applicant-side)/(all-layout)/verify/Content";

export const metadata = {
  title: 'Email Verification - Yahshua HRIS',
  description: 'HRIS',
}

const EmailVerifyPage = () => {
  return <Content />;
};

export default EmailVerifyPage;
